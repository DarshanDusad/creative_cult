import 'package:flutter/material.dart';

class RequestScreen extends StatelessWidget {
  const RequestScreen({Key? key}) : super(key: key);
  static const route = "/request-screen";
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
